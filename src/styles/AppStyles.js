import {StyleSheet} from 'react-native';

const StyleAppGua = StyleSheet.create({
  judulStyle: {
    fontSize: 30,
    textAlign: 'center',
    color: 'red',
    backgroundColor: '#30336b',
  },
  indukStyle: {
    backgroundColor: '#fff',
  },
});

export default StyleAppGua;
